{
  "_comment": "How scripts/generate-examples.sh builds this example from the skill's assets/. namespace = the fixed namespace the <Namespace> placeholder is substituted for; assets = the chosen-approach files to include (Approach A: single-file sitemap — excludes SitemapIndexController.cs to avoid a duplicate /sitemap.xml route).",
  "namespace": "Umbraco.Skills.Examples.Sitemap",
  "assets": [
    "SitemapController.cs",
    "SitemapComposer.cs",
    "SitemapCacheInvalidator.cs"
  ]
}
